# VCS
.git
.gitignore

# Python
**/__pycache__
**/*.pyc
**/*.pyo
*.egg-info
.venv
venv

# Tooling caches
.pytest_cache
.mypy_cache
.ruff_cache
.coverage
htmlcov

# Tests / docs / agent reports / markdown (not needed in the image)
tests
docs
.agents
**/*.md

# Mini App: dependencies and build output are produced inside the image
miniapp/node_modules
miniapp/dist

# Secrets / local env
.env

# Build artifacts
dist
build

# Docker files themselves
Dockerfile
.dockerignore
docker-compose.yml
Caddyfile
